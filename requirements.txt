# ============================================================
# ChatTTS 零样本音色克隆与情感合成流水线 — 依赖清单
# 目标平台: macOS Apple Silicon (M1/M2/M3)
# ============================================================

# --- PyTorch (MPS 后端) ---
torch>=2.0.0
torchaudio>=2.0.0

# --- 音频处理 ---
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0
audiomentations>=0.34.0
pyloudnorm>=0.1.0           # EBU R128 响度归一化

# --- 人声分离 (Demucs) ---
demucs>=4.0.0

# --- 语音增强 (DeepFilterNet 替代 Resemble Enhance) ---
deepfilternet>=0.5.0
noisereduce>=3.0.0

# --- VAD ---
silero-vad>=5.0

# --- 说话人编码器 ---
# wespeaker 从 GitHub 安装: pip install git+https://github.com/wenet-e2e/wespeaker.git
resemblyzer>=0.1.0

# --- TTS (ChatTTS) ---
ChatTTS>=0.0.8

# --- 通用工具 ---
numpy>=1.24.0
scipy>=1.10.0
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.28.0
onnxruntime>=1.15.0

# --- 音频格式支持 ---
ffmpeg-python>=0.2.0